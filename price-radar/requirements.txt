fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
python-dotenv>=1.0
apscheduler>=3.10
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.1
trafilatura>=1.8
requests[socks]>=2.31    # support SOCKS4/5 pour le pool de proxies (PySocks)
curl_cffi>=0.7           # furtivité TLS (empreinte JA3 d'un vrai navigateur)

# --- Cache API (Redis) et PostgreSQL : requis pour docker-compose,
#     inoffensifs en local (l'app retombe sur SQLite + cache mémoire) ---
redis>=5.0
psycopg2-binary>=2.9

# --- Export Excel du module Sources API ---
openpyxl>=3.1

# --- Tests ---
pytest>=8.0

# --- Optionnel : Google Merchant / Content API ---
# google-auth>=2.28

# --- Optionnel : moteurs navigateur furtifs (PLAYWRIGHT_ENGINE) ---
# Choisis-en UN selon .env, puis installe le navigateur associé :
#   stealth    : playwright>=1.42  playwright-stealth>=2.0   (playwright install chromium)
#   patchright : patchright>=1.42                            (patchright install chromium)
#   camoufox   : camoufox[geoip]>=0.4                        (camoufox fetch)
# playwright>=1.42
# playwright-stealth>=2.0
# patchright>=1.42
# camoufox[geoip]>=0.4
