fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
python-dotenv>=1.0
apscheduler>=3.10
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.1
trafilatura>=1.8
requests[socks]>=2.31    # support SOCKS4/5 pour le pool de proxies (PySocks)
curl_cffi>=0.7           # furtivité TLS (empreinte JA3 d'un vrai navigateur)

# --- Tests ---
pytest>=8.0

# --- Optionnel : rendu JavaScript / anti-bot (voir README) ---
# playwright>=1.42
# playwright-stealth>=2.0
# curl_cffi>=0.6
